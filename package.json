{
  "name": "scratchy-par",
  "version": "2.0.0-alpha.1",
  "type": "module",
  "description": "Scratchy v2 — Contextual AI interface with multi-user isolation",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node --test --test-force-exit test/*.test.js",
    "test:verbose": "node --test --test-force-exit --test-reporter spec test/*.test.js"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "license": "ISC",
  "dependencies": {
    "@mozilla/readability": "^0.6.0",
    "better-sqlite3": "^12.6.2",
    "dotenv": "^17.3.1",
    "linkedom": "^0.18.12",
    "openai": "^6.25.0",
    "robots-parser": "^3.0.1",
    "tough-cookie": "^6.0.0",
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "jsdom": "^28.1.0"
  }
}
